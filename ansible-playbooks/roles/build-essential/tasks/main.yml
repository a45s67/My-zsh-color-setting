---
  - name: Install unzip
    apt:
      name: unzip
      update_cache: yes

  - name: Install build-essential
    apt:
      name: build-essential
      update_cache: yes

  - name: Install libs for building tmux
    apt:
      name: 
        - libevent-dev 
        - ncurses-dev
        - bison 
        - pkg-config
      update_cache: yes
